stages:
  - lint
  - test
  - build

default:
  image: rust:1.79.0
  before_script:
    - sed -i "s|ssh://git@git.slock.it/|https://gitlab-ci-token:${CI_JOB_TOKEN}@git.slock.it/|" Cargo.toml

lint-job:
  stage: lint
  script:
    - rustup component add rustfmt
    - rustup component add clippy
    - cargo fmt --all -- --check
    - cargo clippy --workspace --all-targets --all-features -- -Dwarnings
  only:
    - merge_requests
    - main

test-job:
  stage: test
  script:
    - cargo build --all-features
    - cargo test --all-features
  only:
    - merge_requests
    - main

coverage-job:
  stage: test
  before_script:
    - sed -i "s|ssh://git@git.slock.it/|https://gitlab-ci-token:${CI_JOB_TOKEN}@git.slock.it/|" Cargo.toml
    - cargo install cargo-tarpaulin
  script:
    - cargo tarpaulin --timeout 180 --all-features --exclude-files "demos/*" --fail-under 60 -o Html
  coverage: '/^\d+.\d+% coverage/'
  artifacts:
    paths:
      - tarpaulin-report.html
  only:
    - merge_requests
    - main

doc-build-job:
  stage: build
  script:
    - RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --workspace --all-features
  only:
    - merge_requests
    - main

oid4vc-demo-build-job:
  stage: build
  script:
    - cargo build --manifest-path=demos/oid4vc/Cargo.toml
    - cargo build --manifest-path=demos/oid4vc/holder/Cargo.toml
  only:
    - merge_requests
    - main

multi-thread-demo-build-job:
  stage: build
  script:
    - cargo build --manifest-path=demos/multi-thread/Cargo.toml
  only:
    - merge_requests
    - main