[package]
name = "shared"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[dependencies]
equs-sdk = { path = "../../.."}

serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0"
strum_macros = "0.26.4"
uuid = { version = "1.10.0", features = ["v4"] }
